import { NgModule } from '@angular/core';

import { AppMenuToggle } from './app-menu.toggle';

@NgModule({
    declarations: [
        AppMenuToggle
    ],
    exports: [
        AppMenuToggle
    ]
})
export class AppDirectiveModule{}